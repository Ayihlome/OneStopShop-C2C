Minimal Express scaffold for OneStopShop-C2C backend.

To install dependencies and run the server:

1. Open a terminal and change to the backend folder:
   cd backend
2. Install dependencies:
   npm install
3. Start the server:
   npm run start

The server exposes a health endpoint at GET /api/health and a simple users resource at /api/users (in-memory).
