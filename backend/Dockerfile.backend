FROM node:20-alpine

WORKDIR /app

# Needed if your migration scripts use psql
RUN apk add --no-cache postgresql-client

COPY backend/package*.json ./

RUN npm ci --omit=dev

COPY backend ./

EXPOSE 8000

CMD sh -c "npm run migrate && npm run seed:admin && npm start"