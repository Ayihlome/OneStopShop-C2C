# ─── Backend API URL ──────────────────────────────────
# In production (Railway), set this to your backend service URL, e.g.:
#   VITE_API_URL=https://onestopshop-backend.up.railway.app
# In local dev, use the relative path (Vite proxy handles it):
VITE_API_URL=/api
